# 📁 项目目录结构说明

## ✅ 整理完成

项目根目录已整理，所有脚本和文档已分类组织。

## 📂 目录结构

```
项目根目录/
│
├── deploy.bat              # Windows 部署入口（快捷方式）
├── deploy.sh               # Linux/Mac 部署入口（快捷方式）
│
├── scripts/                # 📦 所有脚本文件
│   ├── README.md           # 脚本目录说明
│   │
│   ├── deploy/            # 🚀 部署相关脚本
│   │   ├── deploy-init.bat          # Windows 部署初始化
│   │   ├── deploy-init.sh           # Linux/Mac 部署初始化
│   │   ├── deploy-config.bat.example # Windows 配置示例
│   │   └── deploy-config.sh.example  # Linux/Mac 配置示例
│   │
│   ├── database/          # 💾 数据库 SQL 脚本
│   │   ├── init-database.sql        # 数据库表结构
│   │   └── init-project-data.sql    # 初始数据
│   │
│   └── tools/             # 🔧 工具脚本
│       ├── initialize-mysql.bat      # 初始化 MySQL
│       ├── fix-mysql-service.bat     # 修复 MySQL 服务
│       ├── register-mysql-service.bat # 注册 MySQL 服务
│       ├── create-test-user.ps1     # 创建测试用户
│       ├── create-test-user.sql     # 创建测试用户 SQL
│       └── change-root-password.sql # 修改 root 密码
│
└── docs/                   # 📚 文档
    ├── DEPLOY.md           # 详细部署文档
    ├── README-DEPLOY.md    # 快速部署指南
    ├── README-INIT.md      # 初始化指南
    ├── INIT-SCRIPTS.md     # 脚本说明
    └── 目录结构说明.md     # 本文件
```

## 🎯 快速使用

### 一键部署

在项目根目录执行：

```bash
# Windows
deploy.bat

# Linux/Mac
chmod +x deploy.sh
./deploy.sh
```

### 直接使用脚本

```bash
# Windows
cd scripts\deploy
deploy-init.bat

# Linux/Mac
cd scripts/deploy
./deploy-init.sh
```

## 📋 文件分类说明

### 1. 部署脚本 (`scripts/deploy/`)

**用途：** 生产环境部署，一键初始化数据库

- `deploy-init.bat` / `deploy-init.sh` - 主部署脚本
  - 自动检测 MySQL
  - 创建数据库和表结构
  - 插入初始数据
  - 验证结果

- `deploy-config.*.example` - 配置文件模板
  - 复制为 `deploy-config.bat` 或 `deploy-config.sh`
  - 修改数据库连接信息

### 2. 数据库脚本 (`scripts/database/`)

**用途：** 数据库结构和初始数据

- `init-database.sql` - 创建表结构
  - users 表
  - projects 表
  - pages 表

- `init-project-data.sql` - 插入初始数据
  - 1 个示例项目
  - 3 个页面（首页、关于我们、联系我们）
  - 每个页面包含多个组件数据

### 3. 工具脚本 (`scripts/tools/`)

**用途：** 开发和维护时的辅助工具

- MySQL 服务管理
  - `initialize-mysql.bat` - 初始化 MySQL 数据目录
  - `fix-mysql-service.bat` - 修复 MySQL 服务问题
  - `register-mysql-service.bat` - 注册 MySQL 为 Windows 服务

- 用户管理
  - `create-test-user.ps1` - 通过 API 创建测试用户
  - `create-test-user.sql` - 通过 SQL 创建测试用户
  - `change-root-password.sql` - 修改 root 密码

## 🗑️ 已清理的文件

以下文件已删除（不需要或重复）：

- ❌ `init-all-data.bat` / `init-all-data.sh` - 已删除（与 deploy-init 重复）
- ❌ `insert-test-data.sql` - 已删除（不需要）
- ❌ `query` - 已删除（临时文件）
- ❌ `temp-pom.xml` - 已删除（临时文件）

## 📚 相关文档

- [部署文档](./DEPLOY.md) - 完整部署说明
- [快速指南](./README-DEPLOY.md) - 快速开始
- [脚本说明](./INIT-SCRIPTS.md) - 脚本详细说明

## ✅ 整理效果

**整理前：**
- 根目录有 20+ 个脚本文件
- 文件混乱，不知道用途
- 难以维护

**整理后：**
- 根目录只有 2 个入口文件（deploy.bat / deploy.sh）
- 所有脚本分类到 `scripts/` 目录
- 所有文档分类到 `docs/` 目录
- 结构清晰，易于维护

现在项目结构更加清晰和专业！

