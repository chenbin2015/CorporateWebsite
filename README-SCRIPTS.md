# 📁 项目脚本说明

项目根目录已整理，所有脚本文件已分类组织到 `scripts/` 目录下。

## 📂 目录结构

```
项目根目录/
├── deploy.bat              # Windows 部署入口（快捷方式）
├── deploy.sh               # Linux/Mac 部署入口（快捷方式）
│
├── scripts/                # 所有脚本文件
│   ├── deploy/            # 部署相关
│   │   ├── deploy-init.bat
│   │   ├── deploy-init.sh
│   │   └── deploy-config.*.example
│   │
│   ├── database/          # 数据库 SQL
│   │   ├── init-database.sql
│   │   └── init-project-data.sql
│   │
│   └── tools/             # 工具脚本
│       ├── initialize-mysql.bat
│       ├── fix-mysql-service.bat
│       └── ...
│
└── docs/                   # 文档
    ├── DEPLOY.md
    ├── README-DEPLOY.md
    └── ...
```

## 🎯 使用方法

### 一键部署（最简单）

在项目根目录执行：

```bash
# Windows
deploy.bat

# Linux/Mac
chmod +x deploy.sh
./deploy.sh
```

### 直接使用部署脚本

```bash
# Windows
cd scripts\deploy
deploy-init.bat

# Linux/Mac
cd scripts/deploy
./deploy-init.sh
```

## 📋 脚本分类

### 1. 部署脚本 (`scripts/deploy/`)

**用途：** 生产环境部署，一键初始化数据库

- `deploy-init.bat` / `deploy-init.sh` - 主部署脚本
- `deploy-config.*.example` - 配置文件模板

### 2. 数据库脚本 (`scripts/database/`)

**用途：** 数据库结构和初始数据

- `init-database.sql` - 创建表结构
- `init-project-data.sql` - 插入初始数据（1个项目 + 3个页面）

### 3. 工具脚本 (`scripts/tools/`)

**用途：** 开发和维护时的辅助工具

- MySQL 服务管理脚本
- 用户管理脚本
- 其他工具脚本

## 📚 详细文档

- [部署文档](./docs/DEPLOY.md) - 完整部署说明
- [快速指南](./docs/README-DEPLOY.md) - 快速开始
- [脚本说明](./scripts/README.md) - 脚本详细说明

## ✅ 已清理

以下文件已删除或移动：

- ❌ `init-all-data.bat` / `init-all-data.sh` - 已删除（与 deploy-init 重复）
- ❌ `insert-test-data.sql` - 已删除（不需要）
- ❌ `query` - 已删除（临时文件）
- ❌ `temp-pom.xml` - 已删除（临时文件）
- ✅ 所有脚本已移动到 `scripts/` 目录
- ✅ 所有文档已移动到 `docs/` 目录

现在项目根目录更加整洁，所有脚本都有明确的分类和说明！

