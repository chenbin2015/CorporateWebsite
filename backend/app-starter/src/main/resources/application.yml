server:
  port: 8082

spring:
  datasource:
#    url: jdbc:mysql://localhost:8024/corporate_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
#    username: constweak
#    password: k9#Qz$mR!pX2@L8
    url: jdbc:mysql://localhost:3306/tet?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath*:mappers/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

security:
  jwt:
    secret: please-change-me-very-long-secret
    expiration: 3600000

# 日志配置
logging:
  level:
    root: WARN  # 根日志级别设置为 WARN，只显示警告和错误
    com.company.platform: INFO  # 项目代码日志级别
    org.springframework: WARN  # Spring 框架日志
    org.springframework.security: WARN  # Spring Security 日志
    org.springframework.web: WARN  # Spring Web 日志
    org.mybatis: WARN  # MyBatis 日志
    com.zaxxer.hikari: WARN  # 数据库连接池日志
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"  # 简化日志格式

