<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.platform.infrastructure.user.database.UserMapper">

    <resultMap id="UserResultMap" type="com.company.platform.infrastructure.user.database.model.UserDO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="role" property="role"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT id, username, password, role, created_at
        FROM users
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(1) > 0 FROM users WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.company.platform.infrastructure.user.database.model.UserDO" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO users (username, password, role, created_at)
        VALUES (#{username}, #{password}, #{role}, #{createdAt})
    </insert>

</mapper>


